import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ragav.expensetracker',
  appName: 'Expense-Tracker',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
